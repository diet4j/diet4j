#!/bin/bash
#
# Used to create a downloadable package that can run without maven

VERSION=0.7

tar -c -z -f diet4j-cmdline-${VERSION}.tar.gz bin/diet4j -C target diet4j-cmdline-${VERSION}.jar -C ../.. LICENSE README
